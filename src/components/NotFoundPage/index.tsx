import * as React from 'react'

function NotFoundPage(): React.SFCElement<React.ElementType> {
  return (
    <div style={{textAlign: 'center', fontSize: '32px', color: '#999', lineHeight: '80vh'}}>404 页面不存在</div>
  )
}

export default NotFoundPage